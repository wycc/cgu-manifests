apiVersion: apps/v1
kind: Deployment
metadata:
  name: groupshare-controller
  namespace: kubeflow
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: groupshare-controller
  template:
    metadata:
      labels:
        app: groupshare-controller
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      serviceAccountName: groupshare-controller
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
        - key: node-role.kubernetes.io/master
          operator: Exists
          effect: NoSchedule
      containers:
        - name: controller
          image: docker.io/cguaicadmin/groupshare-controller:v20260617-172109d
          imagePullPolicy: IfNotPresent
          env:
            - name: NFS_NAMESPACE
              value: kf-storage
            - name: NFS_DEPLOYMENT
              value: nfs-subdir-external-provisioner
            - name: NFS_CONFIGMAP
              value: groupshare-nfs-defaults
            - name: GROUPSHARE_PODDEFAULT_NAME
              value: groupshare
            - name: POLL_SECONDS
              value: "20"
